<%= link_to gravatar_for(current_user,
  size: Settings.gravatar_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= t(:microposts_count, count: current_user.microposts.count) %></span>
